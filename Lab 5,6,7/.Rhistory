#year dams removed
addCircleMarkers( data = dams, radius = 5,
fillColor = ~colorNumeric( palette = "YlOrRd",
domain = dams$DamRemoval)(DamRemoval),
fillOpacity = 0.99, color = "black",weight = 1.5,
group = "Dam Removals", popup = ~paste("Year Removed:", DamRemoval)) %>%
#make so flip between layers
addLayersControl(
baseGroups =c("NatGeo", "Light", "Satellite", "BW", "Goth"),
overlayGroups = c("Counties", "Monitoring Stations", "Dam Removals"))
G2
library(tidyverse)
library(dataRetrieval)  # USGS water data
library(tsibble)        # tidy time series data structure
library(feasts)         # decomposition + ACF
library(ggtime)         # time series visualization (split from feasts 0.4.2)
library(fable)          # forecasting models
library(lubridate)
site_no      <- "04208000"   # Cuyahoga River at Independence, OH
parameter_cd <- "00060"      # discharge, in cubic feet per second (cfs)
raw_flow <- dataRetrieval::readNWISdv(
siteNumbers = site_no,
parameterCd = parameter_cd,
startDate   = "2000-01-01",
endDate     = "2023-12-31"
)
head(raw_flow)
raw_flow %>% count(X_00060_00003_cd)
# The package has a helper to rename columns to something readable
flow_clean <- raw_flow %>%
dataRetrieval::renameNWISColumns() %>%
dplyr::select(Date, Flow) %>%
dplyr::filter(!is.na(Flow))
head(flow_clean)
nrow(flow_clean)
# expected days in our date range
expected_days <- as.numeric(as.Date("2023-12-31") - as.Date("2000-01-01")) + 1
expected_days
# how does that compare to what we actually have?
nrow(flow_clean)
# how many NAs were dropped during cleaning?
raw_flow %>% dataRetrieval::renameNWISColumns() %>% pull(Flow) %>% is.na() %>% sum()
library(tidyverse)
library(dataRetrieval)  # USGS water data
library(tsibble)        # tidy time series data structure
library(feasts)         # decomposition + ACF
library(ggtime)         # time series visualization (split from feasts 0.4.2)
library(fable)          # forecasting models
library(lubridate)
library(tidyverse)
library(dataRetrieval)  # USGS water data
library(tsibble)        # tidy time series data structure
library(feasts)         # decomposition + ACF
library(ggtime)         # time series visualization (split from feasts 0.4.2)
library(fable)          # forecasting models
library(lubridate)
site_no      <- "04208000"   # Cuyahoga River at Independence, OH
parameter_cd <- "00060"      # discharge, in cubic feet per second (cfs)
raw_flow <- dataRetrieval::readNWISdv(
siteNumbers = site_no,
parameterCd = parameter_cd,
startDate   = "2000-01-01",
endDate     = "2023-12-31"
)
head(raw_flow)
raw_flow %>% count(X_00060_00003_cd)
# The package has a helper to rename columns to something readable
flow_clean <- raw_flow %>%
dataRetrieval::renameNWISColumns() %>%
dplyr::select(Date, Flow) %>%
dplyr::filter(!is.na(Flow))
head(flow_clean)
nrow(flow_clean)
# expected days in our date range
expected_days <- as.numeric(as.Date("2023-12-31") - as.Date("2000-01-01")) + 1
expected_days
# how does that compare to what we actually have?
nrow(flow_clean)
# how many NAs were dropped during cleaning?
raw_flow %>% dataRetrieval::renameNWISColumns() %>% pull(Flow) %>% is.na() %>% sum()
flow_ts <- flow_clean %>%
tsibble::as_tsibble(index = Date)
flow_ts
flow_ts %>%
feasts::autoplot(Flow) +
labs(
title = "Daily Streamflow — Cuyahoga River at Independence, OH",
y     = "Discharge (cfs)",
x     = NULL
) +
theme_minimal() # see how you can add ggplot "stuff"?
raw_flow %>% count(X_00060_00003_cd)
# The package has a helper to rename columns to something readable
flow_clean <- raw_flow %>%
dataRetrieval::renameNWISColumns() %>%
dplyr::select(Date, Flow) %>%
dplyr::filter(!is.na(Flow))
head(flow_clean)
# expected days in our date range
expected_days <- as.numeric(as.Date("2023-12-31") - as.Date("2000-01-01")) + 1
expected_days
# how does that compare to what we actually have?
nrow(flow_clean)
# how many NAs were dropped during cleaning?
raw_flow %>% dataRetrieval::renameNWISColumns() %>% pull(Flow) %>% is.na() %>% sum()
flow_ts <- flow_clean %>%
tsibble::as_tsibble(index = Date)
flow_ts
flow_ts %>%
feasts::autoplot(Flow) +
labs(
title = "Daily Streamflow — Cuyahoga River at Independence, OH",
y     = "Discharge (cfs)",
x     = NULL
) +
theme_minimal() # see how you can add ggplot "stuff"?
flow_ts %>%
feasts::autoplot(Flow) +
labs(
title = "Daily Streamflow — Cuyahoga River at Independence, OH",
y     = "Discharge (cfs)",
x     = NULL
) +
theme_minimal() # see how you can add ggplot "stuff"?
flow_monthly <- flow_clean %>%
dplyr::mutate(ym = tsibble::yearmonth(Date)) %>%
dplyr::group_by(ym) %>%
dplyr::summarise(mean_flow = mean(Flow, na.rm = TRUE)) %>%
tsibble::as_tsibble(index = ym)
flow_monthly
flow_ts %>%
feasts::autoplot(Flow) +
labs(
title = "Daily Streamflow — Cuyahoga River at Independence, OH",
y     = "Discharge (cfs)",
x     = "year"
) +
theme_minimal() # see how you can add ggplot "stuff"?
flow_monthly %>%
feasts::autoplot(mean_flow) +
labs(
title = "Monthly Mean Streamflow — Cuyahoga River at Independence, OH",
y     = "Mean Discharge (cfs)",
x     = NULL
)
flow_monthly %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym, label = TRUE)) %>%
group_by(month) %>%
summarise(avg_flow = mean(mean_flow, na.rm = TRUE)) %>%
arrange(desc(avg_flow))
flow_monthly %>%
as_tibble() %>%
mutate(year = lubridate::year(ym)) %>%
group_by(year) %>%
summarise(annual_mean = mean(mean_flow, na.rm = TRUE)) %>%
arrange(desc(annual_mean))
flow_monthly %>%
ggtime::gg_subseries(mean_flow) +
labs(
title = "Subseries Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
)
# Is March flow trending over time?
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym),
year  = lubridate::year(ym)) %>%
filter(month == 3) %>%
ggplot(aes(x = year, y = mean_flow)) +
geom_point() +
geom_smooth(method = "lm", se = TRUE) +
labs(title = "March Mean Streamflow Over Time",
y = "Mean Discharge (cfs)", x = "Year")
flow_monthly %>%
filter(year(ym) >= 2010) %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
filter(year(ym) >= 2015) %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
filter(year(ym) >= 2009) %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
filter(year(ym) >= 2020) %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
feasts::ACF(mean_flow, lag_max = 36) %>%
feasts::autoplot() +
labs(title = "Autocorrelation Function — Monthly Mean Streamflow")
n_months <- nrow(flow_monthly)
significance_threshold <- 2 / sqrt(n_months)
significance_threshold
flow_monthly %>%
feasts::ACF(mean_flow, lag_max = 36) %>%
as_tibble() %>%
filter(as.numeric(lag) %in% c(1, 12, 24, 36))
dcmp <- flow_monthly %>%
fabletools::model(
stl = feasts::STL(mean_flow ~ trend(window = 21) + season(window = "periodic"))
)
dcmp %>%
fabletools::components() %>%
feasts::autoplot() +
labs(title = "STL Decomposition — Monthly Mean Streamflow")
# Is March flow trending over time?
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym),
year  = lubridate::year(ym)) %>%
filter(month == 7) %>%
ggplot(aes(x = year, y = mean_flow)) +
geom_point() +
geom_smooth(method = "lm", se = TRUE) +
labs(title = "March Mean Streamflow Over Time",
y = "Mean Discharge (cfs)", x = "Year")
# Is March flow trending over time?
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym),
year  = lubridate::year(ym)) %>%
filter(month == 7) %>%
ggplot(aes(x = year, y = mean_flow)) +
geom_point() +
geom_smooth(method = "lm", se = TRUE) +
labs(title = "____ Mean Streamflow Over Time",
y = "Mean Discharge (cfs)", x = "Year")
n_months <- nrow(flow_monthly)
significance_threshold <- 2 / sqrt(n_months)
significance_threshold
flow_monthly %>%
feasts::ACF(mean_flow, lag_max = 36) %>%
as_tibble() %>%
filter(as.numeric(lag) %in% c(1, 12, 24, 36))
dcmp_components <- dcmp %>% fabletools::components()
dcmp_components %>%
as_tibble() %>%
summarise(
trend_range    = diff(range(trend,       na.rm = TRUE)),
seasonal_range = diff(range(season_year, na.rm = TRUE)),
remainder_range= diff(range(remainder,   na.rm = TRUE))
)
dcmp_components %>%
as_tibble() %>%
select(ym, remainder) %>%
arrange(desc(abs(remainder))) %>%
head(10)
fit <- flow_monthly %>%
fabletools::model(
tslm = fable::TSLM(mean_flow ~ trend() + season())
)
tidy(fit)
# Pull the trend coefficient and express it as monthly and annual change
trend_coef <- tidy(fit) %>% filter(term == "trend()")
trend_coef
# annual rate of change
trend_coef$estimate * 12
# The seasonal coefficients — all relative to the reference month
tidy(fit) %>% filter(stringr::str_detect(term, "season"))
fit %>%
fabletools::augment() %>%
ggplot(aes(x = ym)) +
geom_line(aes(y = mean_flow, color = "Observed")) +
geom_line(aes(y = .fitted,   color = "Fitted")) +
scale_color_manual(values = c("Observed" = "gray50", "Fitted" = "red")) +
labs(
title = "Observed vs. Fitted — TSLM",
y     = "Mean Discharge (cfs)",
x     = NULL,
color = NULL
) + theme_minimal()
library(tidyverse)
library(dataRetrieval)  # USGS water data
library(tsibble)        # tidy time series data structure
library(feasts)         # decomposition + ACF
library(ggtime)         # time series visualization (split from feasts 0.4.2)
library(fable)          # forecasting models
library(lubridate)
site_no      <- "04208000"   # Cuyahoga River at Independence, OH
parameter_cd <- "00060"      # discharge, in cubic feet per second (cfs)
raw_flow <- dataRetrieval::readNWISdv(
siteNumbers = site_no,
parameterCd = parameter_cd,
startDate   = "2000-01-01",
endDate     = "2023-12-31"
)
head(raw_flow)
raw_flow %>% count(X_00060_00003_cd)
# The package has a helper to rename columns to something readable
flow_clean <- raw_flow %>%
dataRetrieval::renameNWISColumns() %>%
dplyr::select(Date, Flow) %>%
dplyr::filter(!is.na(Flow))
head(flow_clean)
nrow(flow_clean)
# expected days in our date range
expected_days <- as.numeric(as.Date("2023-12-31") - as.Date("2000-01-01")) + 1
expected_days
# how does that compare to what we actually have?
nrow(flow_clean)
# how many NAs were dropped during cleaning?
raw_flow %>% dataRetrieval::renameNWISColumns() %>% pull(Flow) %>% is.na() %>% sum()
flow_ts <- flow_clean %>%
tsibble::as_tsibble(index = Date)
flow_ts
flow_ts %>%
feasts::autoplot(Flow) +
labs(
title = "Daily Streamflow — Cuyahoga River at Independence, OH",
y     = "Discharge (cfs)",
x     = "year"
) +
theme_minimal() # see how you can add ggplot "stuff"?
flow_monthly <- flow_clean %>%
dplyr::mutate(ym = tsibble::yearmonth(Date)) %>%
dplyr::group_by(ym) %>%
dplyr::summarise(mean_flow = mean(Flow, na.rm = TRUE)) %>%
tsibble::as_tsibble(index = ym)
flow_monthly
flow_monthly %>%
feasts::autoplot(mean_flow) +
labs(
title = "Monthly Mean Streamflow — Cuyahoga River at Independence, OH",
y     = "Mean Discharge (cfs)",
x     = NULL
)
flow_monthly %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
filter(year(ym) >= 2020) %>%
ggtime::gg_season(mean_flow, labels = "both") +
labs(
title = "Seasonal Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
) + theme_minimal()
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym, label = TRUE)) %>%
group_by(month) %>%
summarise(avg_flow = mean(mean_flow, na.rm = TRUE)) %>%
arrange(desc(avg_flow))
flow_monthly %>%
as_tibble() %>%
mutate(year = lubridate::year(ym)) %>%
group_by(year) %>%
summarise(annual_mean = mean(mean_flow, na.rm = TRUE)) %>%
arrange(desc(annual_mean))
flow_monthly %>%
ggtime::gg_subseries(mean_flow) +
labs(
title = "Subseries Plot — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)"
)
# Is March flow trending over time?
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym),
year  = lubridate::year(ym)) %>%
filter(month == 3) %>%
ggplot(aes(x = year, y = mean_flow)) +
geom_point() +
geom_smooth(method = "lm", se = TRUE) +
labs(title = "March Mean Streamflow Over Time",
y = "Mean Discharge (cfs)", x = "Year")
# Is March flow trending over time?
flow_monthly %>%
as_tibble() %>%
mutate(month = lubridate::month(ym),
year  = lubridate::year(ym)) %>%
filter(month == 7) %>%
ggplot(aes(x = year, y = mean_flow)) +
geom_point() +
geom_smooth(method = "lm", se = TRUE) +
labs(title = "____ Mean Streamflow Over Time",
y = "Mean Discharge (cfs)", x = "Year")
flow_monthly %>%
feasts::ACF(mean_flow, lag_max = 36) %>%
feasts::autoplot() +
labs(title = "Autocorrelation Function — Monthly Mean Streamflow")
n_months <- nrow(flow_monthly)
significance_threshold <- 2 / sqrt(n_months)
significance_threshold
flow_monthly %>%
feasts::ACF(mean_flow, lag_max = 36) %>%
as_tibble() %>%
filter(as.numeric(lag) %in% c(1, 12, 24, 36))
dcmp <- flow_monthly %>%
fabletools::model(
stl = feasts::STL(mean_flow ~ trend(window = 21) + season(window = "periodic"))
)
dcmp %>%
fabletools::components() %>%
feasts::autoplot() +
labs(title = "STL Decomposition — Monthly Mean Streamflow")
dcmp_components <- dcmp %>% fabletools::components()
dcmp_components %>%
as_tibble() %>%
summarise(
trend_range    = diff(range(trend,       na.rm = TRUE)),
seasonal_range = diff(range(season_year, na.rm = TRUE)),
remainder_range= diff(range(remainder,   na.rm = TRUE))
)
dcmp_components %>%
as_tibble() %>%
select(ym, remainder) %>%
arrange(desc(abs(remainder))) %>%
head(10)
fit <- flow_monthly %>%
fabletools::model(
tslm = fable::TSLM(mean_flow ~ trend() + season())
)
tidy(fit)
# Pull the trend coefficient and express it as monthly and annual change
trend_coef <- tidy(fit) %>% filter(term == "trend()")
trend_coef
# annual rate of change
trend_coef$estimate * 12
# The seasonal coefficients — all relative to the reference month
tidy(fit) %>% filter(stringr::str_detect(term, "season"))
fit %>%
fabletools::augment() %>%
ggplot(aes(x = ym)) +
geom_line(aes(y = mean_flow, color = "Observed")) +
geom_line(aes(y = .fitted,   color = "Fitted")) +
scale_color_manual(values = c("Observed" = "gray50", "Fitted" = "red")) +
labs(
title = "Observed vs. Fitted — TSLM",
y     = "Mean Discharge (cfs)",
x     = NULL,
color = NULL
) + theme_minimal()
fit %>%
fabletools::augment() %>%
as_tibble() %>%
mutate(abs_resid = abs(.resid)) %>%
arrange(desc(abs_resid)) %>%
select(ym, mean_flow, .fitted, .resid) %>%
head(10)
fc <- fit %>%
fabletools::forecast(h = "2 years")
fc %>%
fabletools::autoplot(flow_monthly, level = c(80, 95)) +
labs(
title = "2-Year Forecast — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)",
x     = NULL
)
# Last 12 months of observed data
flow_monthly %>% tail(12)
# First 12 months of the forecast
fc %>% as_tibble() %>% select(ym, .mean) %>% head(12)
fit_arima <- flow_monthly %>%
fabletools::model(
arima = fable::ARIMA(mean_flow)
)
report(fit_arima)
fc_arima <- fit_arima %>%
fabletools::forecast(h = "2 years")
fc_arima %>%
fabletools::autoplot(flow_monthly, level = c(80, 95)) +
labs(
title = "2-Year ARIMA Forecast — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)",
x     = NULL
)
fit_both <- flow_monthly %>%
fabletools::model(
tslm  = fable::TSLM(mean_flow ~ trend() + season()),
arima = fable::ARIMA(mean_flow)
)
fit_both %>%
fabletools::forecast(h = "2 years") %>%
fabletools::autoplot(flow_monthly, level = NULL) +
labs(
title = "TSLM vs. ARIMA Forecast — Monthly Mean Streamflow",
y     = "Mean Discharge (cfs)",
x     = NULL
)
fit_both %>% fabletools::accuracy()
# Residual plot for ARIMA
fit_arima %>%
fabletools::augment() %>%
ggplot(aes(x = ym, y = .resid)) +
geom_line() +
geom_hline(yintercept = 0, linetype = "dashed", color = "red") +
labs(title = "ARIMA Residuals", y = "Residual (cfs)", x = NULL)
