do.call(rbind, .)
# your code here
shp_files <- list.files("../data/wildcard_thursday/erie_raster_ex/bounds/",
pattern = "\\.shp$", full.names = TRUE)
# read as sf + add name column
aois <- shp_files %>%
purrr::map(~ sf::read_sf(.x) %>%
dplyr::mutate(aoi_name = basename(.x))) %>%
dplyr::bind_rows()
aois <- sf::st_as_sf(aois)
aois_vect <- terra::vect(aois)
aois
aois_vect
# your code here
shp_files <- list.files("../data/wildcard_thursday/erie_raster_ex/bounds/",
pattern = "\\.shp$", full.names = TRUE)
# your code here
shp_files <- list.files("../erie_raster_ex/bounds/",
pattern = "\\.shp$", full.names = TRUE)
# your code here
shp_files <- list.files("./erie_raster_ex/bounds/", pattern = "\\.shp$", full.names = TRUE)
# read as sf + add name column
aois <- shp_files %>%
purrr::map(~ sf::read_sf(.x) %>%
dplyr::mutate(aoi_name = basename(.x))) %>%
dplyr::bind_rows()
aois <- sf::st_as_sf(aois)
aois_vect <- terra::vect(aois)
aois
aois_vect
# FIX geometry (important)
aois <- sf::st_as_sf(aois)
aois <- sf::st_make_valid(aois)
# your code here
shp_files <- list.files("./erie_raster_ex/bounds/",
pattern = "\\.shp$", full.names = TRUE)
aois <- shp_files %>%
purrr::map(~ sf::read_sf(.x) %>%
dplyr::mutate(aoi_name = basename(.x))) %>%
dplyr::bind_rows()
# FIX geometry (important)
aois <- sf::st_as_sf(aois)
aois <- sf::st_make_valid(aois)
# your code here
shp_files <- list.files("./erie_raster_ex/bounds/",
pattern = "\\.shp$", full.names = TRUE)
aois <- shp_files %>%
purrr::map(~ {
x <- sf::read_sf(.x)
# fix geometry BEFORE combining
x <- sf::st_make_valid(x)
# add name
x <- dplyr::mutate(x, aoi_name = basename(.x))
return(x)
}) %>%
dplyr::bind_rows()
# convert to terra
aois_vect <- terra::vect(aois)
# check
aois
aois_vect
# your code here
vals <- terra::extract(best_raster, aois_vect)
summary_stats <- vals %>%
dplyr::group_by(ID) %>%
dplyr::summarize(
mean_ci = mean(layer, na.rm = TRUE),
max_ci = max(layer, na.rm = TRUE)
)
# your code here
vals <- terra::extract(best_raster, aois_vect)
summary_stats <- vals %>%
dplyr::group_by(ID) %>%
dplyr::summarize(
mean_ci = mean(layer, na.rm = TRUE),
max_ci = max(layer, na.rm = TRUE)
)
# your code here
shp_files <- list.files("./erie_raster_ex/bounds/",
pattern = "\\.shp$", full.names = TRUE)
aois <- shp_files %>%
purrr::map(~ {
x <- sf::read_sf(.x)
x <- sf::st_make_valid(x)
x <- dplyr::mutate(x, aoi_name = basename(.x))
return(x)
}) %>%
dplyr::bind_rows()
# convert to terra
aois_vect <- terra::vect(aois)
# check
aois
aois_vect
# your code here
vals <- terra::extract(best_raster, aois_vect)
summary_stats <- vals %>%
dplyr::group_by(ID) %>%
dplyr::summarize(
mean_ci = mean(layer, na.rm = TRUE),
max_ci = max(layer, na.rm = TRUE)
)
# your code here
vals <- terra::extract(best_raster, aois_vect)
summary_stats <- vals %>%
dplyr::group_by(ID) %>%
dplyr::summarize(
mean_ci = mean(.[[2]], na.rm = TRUE),
max_ci = max(.[[2]], na.rm = TRUE)
)
View(summary_stats)
# your code here
summary_stats
vals <- vals %>%
dplyr::rename(ci = 2)
summary_stats <- vals %>%
dplyr::group_by(ID) %>%
dplyr::summarize(
mean_ci = mean(ci, na.rm = TRUE),
max_ci = max(ci, na.rm = TRUE)
)
# your code here
summary_stats
tm_shape(best_raster) +
tm_raster(title = "CI") +
tm_shape(aois) +
tm_borders(col = "black") +
tm_layout(title = "Cyanobacteria Index with AOIs")
#load librarys ----
library(tidyverse)
library(sf)
library(ggplot2)
library(tmap)
library(RColorBrewer)
library(spdep)
library(tidycensus)
library (tigris)
source("~/Documents/GitHub/Environmental-analysis-class/EA_Labs/Lab_3_Turner.R")
#task 4 chlorpleth map ----
map_mode("plot")
# task 3 histogram of variable ----
ggplot(data_subset, aes(x = pct_f75_79)) +
geom_histogram(bins = 30, fill = "darkgreen", color = "black", linewidth = 0.4) +
theme_minimal() + labs(title = "Percent Females 75–79")
#task 4 chlorpleth map ----
map_mode("plot")
tm_shape(data_subset) +
tm_polygons( "pct_f75_79", style = "quantile",
palette = "Greens", title = "% Female 75–79") +
tm_shape(states_sf) +
tm_borders(col = "black", lwd = 2)
tm_shape(data_subset) +
tm_polygons( "pct_f75_79", style = "quantile",
palette = "Greens", title = "% Female 75–79") +
tm_shape(states_sf) +
tm_borders(col = "black", lwd = 2)
#task 4 chlorpleth map ----
tmap_mode("plot")
tm_shape(data_subset) +
tm_polygons( "pct_f75_79", style = "quantile",
palette = "Greens", title = "% Female 75–79") +
tm_shape(states_sf) +
tm_borders(col = "black", lwd = 2)
validdata = terra::classify(myras, rcl = rcl)
# binary rasters: 1 = bloom present, 0 = no bloom
b_early <- terra::ifel(!is.na(r_early), 1, 0)
# your code here
tmap_mode("plot")
tm_shape(sandusky_0816) +
tm_raster(title = "Cyanobacteria Index") +
tm_layout(title = "CI in Sandusky Bay (Aug 16–22, 2017)") +
tm_scale_bar()
install.packages("spData")
library(sf)
library(terra)
library(tmap)
library(spData)      # demo datasets (nz, nz_height, etc.)
library(tidyverse)
# install only once — leave commented after initial install
install.packages("spDataLarge", repos = "https://nowosad.github.io/drat/", type = "source")
library(spDataLarge)
nz_elev <- rast(system.file("raster/nz_elev.tif", package = "spDataLarge"))
spData::nz
spData::nz
tm_shape(nz) +
tm_fill()
tm_shape(nz) +
tm_borders()
tm_shape(nz) +
tm_fill() +
tm_borders()
# borders THEN fill — the fill is drawn on top of the borders, so borders disappear
tm_shape(nz) +
tm_borders() +
tm_fill()
tm_shape(nz) +
tm_fill(lightgreen)
tm_shape(nz) +
tm_fill("lightgreen")
tm_shape(nz) +
tm_fill("darkgreen") +
tm_borders()
tm_shape(nz) +
tm_fill("green") +
tm_borders()
map_nz_base <- tm_shape(nz) + tm_polygons()
map_nz_blue_dots <- map_nz_base +
tm_shape(nz_height) + tm_dots(size = 0.25, fill = "blue")
map_nz_blue_dots
map_nz_base <- tm_shape(nz) + tm_polygons()
map_nz_blue_dots <- map_nz_base +
tm_shape(nz_height) + tm_dots(size = 0.1, fill = "blue")
map_nz_blue_dots
map_nz_base <- tm_shape(nz) + tm_polygons()
map_nz_blue_dots <- map_nz_base +
tm_shape(nz_height) + tm_dots(size = 0.15, fill = "blue")
map_nz_blue_dots
map_nz_base <- tm_shape(nz) + tm_polygons()
map_nz_blue_dots <- map_nz_base +
tm_shape(nz_height) + tm_dots(size = 0.25, fill = "blue")
map_nz_blue_dots
map_nz_base <- tm_shape(nz) + tm_polygons()
map_nz_blue_dots <- map_nz_base +
tm_shape(nz_height) +
tm_dots(size = 0.25, fill = "blue")
map_nz_blue_dots
map_nz_red_bubbles <- map_nz_base +
tm_shape(nz_height) + tm_bubbles(size = "elevation",
fill = "darkred",
lwd = 0,
fill_alpha = .4)
map_nz_red_bubbles
tm_shape(nz) + tm_polygons(fill = "Median_income")
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(breaks = c(0, 30000, 40000, 50000)))
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(n = 10))
hist(nz$Median_income)
hist(nz$Median_income)
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(breaks = c(0, 30000, 40000, 50000)))
hist(nz$Median_income)
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(breaks = c(0, 30000, 40000, 50000)))
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(n = 10))
# Jenks (natural breaks): minimizes within-class variance
# Good for: data with natural clusters or groupings; often the most "honest" method
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale_intervals(style = "jenks", values = "viridis"))
# Quantile: puts an equal NUMBER of observations in each class
# Good for: comparing ranks; always produces full-color maps even with skewed data
# Watch out: equal counts can put very different values in the same class
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale_intervals(style = "quantile", values = "brewer.purples", n = 5))
# Equal interval: divides the data RANGE into equal-width bins
# Good for: uniformly distributed data; when the range itself is meaningful
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale_intervals(style = "equal", values = "BuGn"))
legend_title <- expression("Area (km"^2*")")
tm_shape(nz) +
tm_polygons(fill = "Land_area", fill.legend = tm_legend(title = legend_title))
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
orientation = "landscape",
position = tm_pos_out("center", "bottom")))
legend_title <- expression("Area (km"^2*")")
tm_shape(nz) +
tm_polygons(fill = "Land_area", fill.legend = tm_legend(title = legend_title))
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
orientation = "landscape",
position = tm_pos_out("center", "bottom")))
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
orientation = "landscape",
position = tm_pos_out("center", "bottom")))
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
position = tm_pos_out("center", "bottom")))
tmap_mode(mode = "view")
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
position = tm_pos_out("center", "bottom")))
ttm()  # switch back to static mode
tmap_mode(mode = "view")
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
position = tm_pos_out("center", "bottom")))
ttm()  # switch back to static mode
ttm()  # switch back to static mode
map1 <- tm_shape(nz) + tm_polygons() +
tm_shape(nz_elev) + tm_raster(col_alpha = 0.7)
map1
ttm()  # switch back to static mode
map1 <- tm_shape(nz) + tm_polygons() +
tm_shape(nz_elev) + tm_raster(col_alpha = 0.7)
map1
nz_water <- st_union(nz) %>%
st_buffer(22200) %>%       # 22200 meters — we know units from st_crs(nz)$units
st_cast(to = "LINESTRING")
map2 <- tm_shape(nz_water) + tm_lines()
map2
map3 <- tm_shape(nz_height) + tm_symbols()
map3
map.all <- map1 + map2 + map3
map.all
map.arr <- tmap_arrange(map1, map2, map3)
map.arr
ma1 <- tm_shape(nz) + tm_polygons(fill = "red")
ma2 <- tm_shape(nz) + tm_polygons(fill = "red", fill_alpha = 0.3)
ma3 <- tm_shape(nz) + tm_polygons(col = "blue")
ma4 <- tm_shape(nz) + tm_polygons(lwd = 3)
ma5 <- tm_shape(nz) + tm_polygons(lty = 2)
ma6 <- tm_shape(nz) + tm_polygons(fill = "red", fill_alpha = 0.3,
col = "blue", lwd = 3, lty = 2)
tmap_arrange(ma1, ma2, ma3, ma4, ma5, ma6)
allohio <- sf::read_sf("../data/static_mapping/oh_counties.gpkg")
sa <- allohio %>% dplyr::filter(NAME %in% c("Portage", "Summit"))
library(sf)
library(terra)
library(tmap)
library(spData)      # demo datasets (nz, nz_height, etc.)
library(tidyverse)
# install only once — leave commented after initial install
#install.packages("spDataLarge", repos = "https://nowosad.github.io/drat/", type = "source")
library(spDataLarge)
nz_elev <- rast(system.file("raster/nz_elev.tif", package = "spDataLarge"))
spData::nz
tm_shape(nz) +
tm_fill("lightgreen")
tm_shape(nz) +
tm_borders()
tm_shape(nz) +
tm_fill("green") +
tm_borders()
# borders THEN fill — the fill is drawn on top of the borders, so borders disappear
tm_shape(nz) +
tm_borders() +
tm_fill()
map_nz_base <- tm_shape(nz) + tm_polygons()
map_nz_blue_dots <- map_nz_base +
tm_shape(nz_height) +
tm_dots(size = 0.25, fill = "blue")
map_nz_blue_dots
map_nz_red_bubbles <- map_nz_base +
tm_shape(nz_height) + tm_bubbles(size = "elevation",
fill = "darkred",
lwd = 0,
fill_alpha = .4)
map_nz_red_bubbles
tm_shape(nz) + tm_polygons(fill = "Median_income")
hist(nz$Median_income)
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(breaks = c(0, 30000, 40000, 50000)))
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale(n = 10))
# Equal interval: divides the data RANGE into equal-width bins
# Good for: uniformly distributed data; when the range itself is meaningful
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale_intervals(style = "equal", values = "BuGn"))
# Jenks (natural breaks): minimizes within-class variance
# Good for: data with natural clusters or groupings; often the most "honest" method
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale_intervals(style = "jenks", values = "viridis"))
# Quantile: puts an equal NUMBER of observations in each class
# Good for: comparing ranks; always produces full-color maps even with skewed data
# Watch out: equal counts can put very different values in the same class
tm_shape(nz) + tm_polygons(fill = "Median_income",
fill.scale = tm_scale_intervals(style = "quantile", values = "brewer.purples", n = 5))
legend_title <- expression("Area (km"^2*")")
tm_shape(nz) +
tm_polygons(fill = "Land_area", fill.legend = tm_legend(title = legend_title))
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
orientation = "landscape",
position = tm_pos_out("center", "bottom")))
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
position = tm_pos_out("center", "bottom")))
tmap_mode(mode = "view")
tm_shape(nz) +
tm_polygons(fill = "Land_area",
fill.legend = tm_legend(title = legend_title,
position = tm_pos_out("center", "bottom")))
ttm()  # switch back to static mode
map1 <- tm_shape(nz) + tm_polygons() +
tm_shape(nz_elev) + tm_raster(col_alpha = 0.7)
map1
nz_water <- st_union(nz) %>%
st_buffer(22200) %>%       # 22200 meters — we know units from st_crs(nz)$units
st_cast(to = "LINESTRING")
map2 <- tm_shape(nz_water) + tm_lines()
map2
map3 <- tm_shape(nz_height) + tm_symbols()
map3
map.all <- map1 + map2 + map3
map.all
map.arr <- tmap_arrange(map1, map2, map3)
map.arr
ma1 <- tm_shape(nz) + tm_polygons(fill = "red")
ma2 <- tm_shape(nz) + tm_polygons(fill = "red", fill_alpha = 0.3)
ma3 <- tm_shape(nz) + tm_polygons(col = "blue")
ma4 <- tm_shape(nz) + tm_polygons(lwd = 3)
ma5 <- tm_shape(nz) + tm_polygons(lty = 2)
ma6 <- tm_shape(nz) + tm_polygons(fill = "red", fill_alpha = 0.3,
col = "blue", lwd = 3, lty = 2)
tmap_arrange(ma1, ma2, ma3, ma4, ma5, ma6)
allohio <- sf::read_sf("../data/static_mapping/oh_counties.gpkg")
sa <- allohio %>% dplyr::filter(NAME %in% c("Portage", "Summit"))
tm_shape(allohio) + tm_polygons() +
tm_shape(sa) + tm_polygons(fill = "steelblue") +
tm_scalebar(position = c("left", "bottom"))
myras <- rast("../data/static_mapping/neoh_dem.tif") %>%
terra::project(., sf::st_crs(sa)$wkt) %>%. # note that terra uses "project" instead of "transform"
myras <- rast("./data/static_mapping/neoh_dem.tif") %>%
terra::project(., sf::st_crs(sa)$wkt) %>%. # note that terra uses "project" instead of "transform"
myras <- rast("../data/static_mapping/neoh_dem.tif") %>%
terra::project(., sf::st_crs(sa)$wkt) %>%. # note that terra uses "project" instead of "transform"
myras <- rast("../data/static_mapping/neoh_dem.tif") %>%
terra::project(., sf::st_crs(sa)$wkt) %>% # note that terra uses "project" instead of "transform"
terra::crop(., sa)
tm_shape(myras) +
tm_raster(col.scale = tm_scale_continuous(values = "YlGn"),
col.legend = tm_legend(position = c("left", "top"))) +
tm_shape(sa) + tm_polygons(fill_alpha = 0.2) +
tm_scalebar(position = c("left", "bottom"))
library(grid)  # base R package — no install needed
norm_dim <- function(obj) {
bbox   <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)   # ≤ 1.0: fraction of longer side
h <- height / max(width, height)   # ≤ 1.0: fraction of longer side
return(unit(c(w, h), "snpc"))      # "snpc" = square normalized parent coordinates
}
main_dim <- norm_dim(sa)        # Portage + Summit: the MAIN (detail) map
ins_dim  <- norm_dim(allohio)   # All Ohio: the INSET (context) map
main_dim
ins_dim
oh_region <- st_bbox(sa) %>% st_as_sfc()
map_inset <- tm_shape(allohio) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 3, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
norm_dim <- function(obj) {
bbox   <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)   # ≤ 1.0: fraction of longer side
h <- height / max(width, height)   # ≤ 1.0: fraction of longer side
return(unit(c(w, h), "snpc"))      # "snpc" = square normalized parent coordinates
}
# your code here
tmap_mode("plot")
tm_shape(sandusky_0816) +
tm_raster(title = "Cyanobacteria Index") +
tm_layout(title = "CI in Sandusky Bay (Aug 16–22, 2017)") +
tm_scale_bar()
main_dim <- norm_dim(sa)        # Portage + Summit: the MAIN (detail) map
ins_dim  <- norm_dim(allohio)   # All Ohio: the INSET (context) map
main_dim
ins_dim
norm_dim <- function(obj) {
bbox   <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)   # ≤ 1.0: fraction of longer side
h <- height / max(width, height)   # ≤ 1.0: fraction of longer side
return(unit(c(w, h), "snpc"))      # "snpc" = square normalized parent coordinates
}
main_dim <- norm_dim(sa)        # Portage + Summit: the MAIN (detail) map
ins_dim  <- norm_dim(allohio)   # All Ohio: the INSET (context) map
main_dim
ins_dim
oh_region <- st_bbox(sa) %>% st_as_sfc()
map_inset <- tm_shape(allohio) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 3, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,              # Ohio's normalized width, scaled to 40% of main map
height = ins_dim[2] * 0.4,              # Ohio's normalized height, scaled proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # position: right edge minus 0.5cm margin
y      = unit(0.01, "npc"),             # position: 1% up from the bottom
just   = c("right", "bottom")           # which corner of the inset does x,y refer to?
)
grid.newpage()
print(oh_height_map, vp = main_vp)   # draw main map
grid.newpage()
print(oh_height_map, vp = main_vp)   # draw main map
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,              # Ohio's normalized width, scaled to 40% of main map
height = ins_dim[2] * 0.4,              # Ohio's normalized height, scaled proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # position: right edge minus 0.5cm margin
y      = unit(0.01, "npc"),             # position: 1% up from the bottom
just   = c("right", "bottom")           # which corner of the inset does x,y refer to?
)
grid.newpage()
print(oh_height_map, vp = main_vp)   # draw main map
